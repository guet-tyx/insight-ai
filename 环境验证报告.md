# Insight AI 环境配置与验证报告

日期：2026-08-10
目标：按《计划.txt》第十二周实施路线，完成阶段一（W1-W4）所需全部基础环境搭建与验证。

## 一、Windows vs WSL 方案权衡结论

### 权衡过程

| 评估维度 | 全 Windows 原生 | 全 WSL2 (Ubuntu) | ✅ 采用：Windows 原生 + Docker Desktop(WSL2 后端) |
|---|---|---|---|
| 工具链现状 | Python/uv/Node/pnpm 已装，零重装 | WSL 内仅有系统 Python 3.10，需全部重装 | 直接复用 Windows 工具链 |
| 容器（Milvus/Neo4j/Redis） | **仅 Linux 镜像**，必须借助 WSL 虚拟机 | Docker Engine 装进 WSL | Docker Desktop 底层就是 WSL2，Linux 容器照跑 |
| 项目目录 D:\agent开发\insigtai | 直接读写，I/O 快 | 经 /mnt/d 9p 文件系统，node_modules/git 慢 5-10x | 留在 D:，性能无损耗 |
| 网络代理 (127.0.0.1:7897) | 系统代理自动生效 | NAT 模式不镜像 localhost 代理，需手动配置 | 无需额外配置（Docker 拉镜像走系统代理） |
| Playwright/Chromium | 原生支持，已验证 | 需装 libnss3 等系统依赖 | Windows 原生 Chromium |
| 生产一致性 | 差 | 好 | 中（通过 Docker Compose 构建验证，与云部署产物一致） |

### 结论
- **WSL2 无法绕开**：计划中的 Milvus/Neo4j/Redis 只有 Linux 容器镜像，必须跑在 WSL2 虚拟机里；Docker Desktop 的 WSL2 后端即为此而设。
- **代码与工具链放 Windows**：uv/Node 已装好、项目在 D:、Playwright 原生可用，避免 9p 文件系统性能损耗与代理镜像问题。
- 已存在的 `Ubuntu-22.04` WSL 发行版保持不动，仅供未来需要 Linux 原生调试时使用。

## 二、安装清单与版本

| 组件 | 版本 | 安装方式 | 状态 |
|---|---|---|---|
| Docker Desktop | 4.85.0 (引擎 29.6.2) | winget | ✅ 已验证 |
| Docker Compose | v5.3.1 | 随 Docker Desktop | ✅ 已验证 |
| Python（项目） | 3.12.13 | uv python install | ✅ 固定于 .python-version |
| uv | 0.11.6 | 系统已有 | ✅ |
| Node.js | 24.14.1 | 系统已有 | ✅ |
| pnpm | 11.17.0 | 系统已有 | ✅（已放行构建脚本） |
| Playwright Chromium | chromium-1234 (+headless_shell) | playwright install | ✅ 冒烟测试通过 |

## 三、Python 核心依赖（backend/.venv，uv 管理）

| 包 | 版本 | 用途（对应计划周次） |
|---|---|---|
| langgraph | 1.2.10 | 多智能体编排（W4/W5/W8） |
| langchain-core / langchain-openai / langchain-text-splitters | 1.5.3 | RAG 链路（W2） |
| fastapi + uvicorn[standard] | — | 后端 API（W1） |
| fastmcp | 3.4.6 | MCP 工具服务化（W9） |
| pymupdf | 1.28.2 | 版面感知 PDF 解析（W2） |
| pymilvus | 3.0.1 | 向量检索（W2/W7） |
| neo4j | 6.2.0 | 图查询（W7） |
| browser-use | 0.11.13 | 浏览器 Agent（W3/W6） |
| playwright | — | 浏览器驱动（W3） |
| langgraph-checkpoint-redis | — | HITL 检查点持久化（W8） |
| redis | 7.4.1 | 状态存储（W8） |
| pyjwt / python-multipart / feedparser / httpx | — | JWT 认证 / 上传 / RSS / HTTP（W1-W3） |

> 注：redis 锁为 `7.4.1`（<8），因 langgraph-checkpoint-redis 依赖约束；计划中的 "Pydantic v3" 在当前生态下解析为 pydantic 2.13.4（v2 最新版），langgraph/fastmcp 均依赖 v2 系，功能不受影响。

## 四、基础设施容器（docker-compose.yml）

| 服务 | 镜像 | 端口 | 健康状态 |
|---|---|---|---|
| insight-milvus | milvusdb/milvus:v2.5.9 (standalone) | 19530 (gRPC), 9091 | ✅ healthy |
| insight-etcd | quay.io/coreos/etcd:v3.5.18 | 内部 | ✅ healthy |
| insight-minio | minio/minio:RELEASE.2025-04-22T22-12-26Z | 9000, 9001 | ✅ healthy |
| insight-neo4j | neo4j:5.26-community | 7474 (HTTP), 7687 (Bolt) | ✅ healthy |
| insight-redis | redis:7-alpine | 6379 | ✅ healthy |

### 连通性实测（Windows Python → 容器）
- **Redis**：`PING → PONG` ✅
- **Milvus**：建集合(dim=8) → 插入 → flush → 向量搜索命中(cosine dist=1.0) → 删集合 ✅
- **Neo4j**：verify_connectivity → 写入 `(Company)-[:DEVELOPED]->(Technology)` → 拓扑查询 → 清理 ✅（密码 `insightai-neo4j`）

## 五、前端骨架（frontend/）

- Next.js **14.2.31** + React 18 + TypeScript + Tailwind CSS 3.4 + App Router（src 目录）
- `pnpm build` 生产构建通过（首页静态预渲染 92.4 kB）

## 六、常用命令速查

```bash
# 容器生命周期（项目根目录）
docker compose up -d          # 启动全部基础设施
docker compose down           # 停止（保留数据卷）
docker compose ps             # 查看健康状态

# 后端（backend/）
uv run uvicorn app.main:app --reload --port 8000

# 前端（frontend/）
pnpm dev                      # http://localhost:3000

# 浏览器
uv run playwright install chromium   # 已安装，重装可复用缓存
```

## 七、待配置项（不在环境安装范围）

1. **LLM API Key**：复制 `.env.example` 为 `.env`，填写 `OPENAI_API_KEY`（推理 + Embedding）、`LANGCHAIN_API_KEY`（W11 可观测性）。当前无 Key，代码编写阶段将以接口抽象 + 配置注入方式开发，不硬编码。
2. **GitHub 仓库**：W1 任务（本次未执行，属实施周次范围）。
3. **WSL 提示**：`wsl -l -v` 显示 Ubuntu-22.04 正常；NAT 模式不镜像 localhost 代理，若在 WSL 内使用网络需单独配置代理 —— 本方案不依赖 WSL 内网络，无影响。

## 八、风险备注

- Docker Desktop 的 WSL 虚拟机内存默认上限 50% 主机内存（本机 7.4GiB）；Milvus+Neo4j+Redis 实测占用约 2-3GB，余量充足；若后续并发压测可调 `.wslconfig` 的 `memory` 上限。
- MinIO/etcd 仅内网暴露（MinIO 控制台 9001 除外），未配置鉴权以外的访问控制 —— 仅限本机开发使用。

## 九、嵌入链路实测与避坑记录（2026-08-10）

**SiliconFlow BAAI/bge-m3 调用方式**（OpenAI 兼容协议）：
- Endpoint：`POST https://api.siliconflow.cn/v1/embeddings`，鉴权 `Authorization: Bearer <key>`
- 维度 1024、多语言、最长 8192 tokens；已验证 API 确定性（同文本两次调用 cosine=1.0）、批内/跨批一致性（0.9999）

**实测结论**：`embeddings → Milvus 写入 → 中文 Query 检索` 全链路通过，Top1 正确命中（score 0.75）。

**⚠️ 关键坑：langchain-openai 与兼容服务的 token 化冲突**
- 现象：`OpenAIEmbeddings` 默认配置下，检索 Top1 命中无关文档（问 "Insight AI 情报分析平台是什么" 却匹配到 "Neo4j 用 Cypher..."），且任意文本间余弦相似度异常偏高（0.6-0.7）
- 根因：langchain-openai 1.x 默认 `check_embedding_ctx_length=True`，会本地用 tiktoken 将文本转为 **token ID 数组**作为 `input` 发送（该路径为 OpenAI 官方 API 设计的长度控制机制）；硅基流动等兼容服务不会报错，但按 token ID 直接嵌入，向量语义完全失真
- 修复：构造时传 `check_embedding_ctx_length=False`，发送原始文本字符串
- 排查手段（记录备查）：拦截 `httpx.Client.send` 打印请求体比对；用原生 httpx 直调 API 验证嵌入质量以隔离框架层问题
- 防回归：`backend/scripts/verify_embedding.py` 已内置该配置，后续所有嵌入调用点必须显式传此参数

## 十、W3 浏览器采集模块实测记录（2026-08-10）

**browser-use 0.11.13 与计划示例（0.2.x）的 API 差异（已适配）**：
- `Browser/BrowserConfig/browser_context` → `BrowserSession(headless, args, headers, keep_alive)`
- Agent 输出：`history.final_result`（文本）/ `history.structured_output`（Pydantic）
- ⚠️ **LLM 必须用 `browser_use.llm.openai.ChatOpenAI`**（框架已从 langchain 迁移到
  openai.types 协议；langchain 的 ChatOpenAI 缺 `provider` 属性会在 Agent 初始化直接报错）
- ⚠️ 模型非视觉 → `use_vision=False` 走 DOM 文本路线（正合计划 Token 压缩目标）

**实测链路（全部通过）**：
- CLI（`scripts/demo_collect.py --internal-demo`）：本地测试页 → 结构化输出
  标题「AI Agent 行业动态速览」+ 4 条要点 ✅
- API：`POST /api/v1/collect`（202）→ 轮询 → example.com 真实外网采集
  `{"title": "Example Domain"}` ✅；内网地址 SSRF 拦截 422 ✅（pytest 断言）
- 反检测基线：随机 UA 注入 + 代理池配置驱动轮换（`.env BROWSER_PROXY_LIST`）+ 指数退避重试
- pytest 46/46 全绿（新增 20 单元 + 4 集成）

## 十一、阶段一里程碑达成：W4 单 Agent + SSE 联调（2026-08-10）

**端到端 MVP 链路实测（用户提问 → Agent 调工具 → 流式答案）**：
- SSE 事件序列：`token → tool_start(knowledge_search) → tool_end → token… → done` ✅
- GUI 走查（浏览器自动化 + 截图 + 识图模型断言）：注册/登录 → 提问
  「知识库中介绍了哪些检索技术？」→ AI 回答（HNSW/Cypher/RRF，带 [1] 引用）
  + 黄色工具卡 ✓ knowledge_search + 溯源预览，无报错 ✅

**踩坑记录（均已修复）**：
1. `create_react_agent` 的模型节点名是 **"agent"** 而非 "model" —— 用
   `metadata["langgraph_node"]` 过滤 token 事件时漏掉全部输出
2. **每次请求新建 agent 会丢失多轮记忆**（MemorySaver 实例不共享）→
   必须模块级单例 `get_agent()`；多进程部署需切 RedisSaver（W5）
3. React StrictMode 双调用 setState updater → updater 内不可持有可变闭包
   变量（当前助手消息索引），否则第二次调用崩溃 —— 改为纯 updater 就地更新

**质量**：pytest 53/53 全绿（新增 7 例 chat：工具注册/reasoning 过滤/鉴权/
端到端工具流/多轮记忆「猎鹰」）；前端 `pnpm build` 通过

## 十二、W5 多智能体编排实测记录（2026-08-10）

**基础设施变更**：
- Redis 镜像 `redis:7-alpine` → `redis/redis-stack-server:7.2.0-v10`
  （langgraph-checkpoint-redis 依赖 RediSearch 模块；旧 RDB 不兼容 → 清空 dev 数据卷）
- Docker Engine 配置 registry-mirrors（docker.1ms.run 等）解决直连 registry 超时
- 检查点后端：**AsyncRedisSaver**（7 天 TTL），Redis 不可用自动降级 MemorySaver；
  `CHECKPOINTER_BACKEND=memory` 供测试环境强制内存（pytest 多事件循环下
  AsyncRedisSaver 连接复用会报「事件循环已关闭」，持久化由独立用例覆盖）

**交付**（`backend/app/agents/`）：
- `state.py`：GlobalState + 三子图私有状态（add_messages / operator.add Reducer）
- `supervisor.py`：结构化路由（with_structured_output，一次调用完成意图识别）
  + **MAX_ITERATIONS=6 熔断**（计划风险表落地）
- 三 worker 子图：collector（W3 采集）/ research（W2 检索）/ analyst（LLM 报告，[1] 引用）
- `api/v1/agents.py`：POST /runs（202）+ GET /runs/{id}/stream（SSE stage/done）
  + GET /runs/{id}（状态）
- `docs/agents-design.md`：拓扑图/状态表/时序/熔断/与计划交付项对照

**实测链路**（全部通过）：
- CLI：`采集 example.com 页面并生成一段简要分析` → Supervisor→collector✓
  →analyst→finish，报告准确引用 IANA 保留域名事实（[1]）
- API SSE：401 鉴权 → 202 → stage(research/analyst/finish) → done 报告
- pytest **62/62**（新增 9 例：拓扑/熔断/隔离/完整链路/RedisSaver 跨实例持久化）

**踩坑记录**：
1. **AsyncRedisSaver 事件循环绑定**（关键）：连接绑定创建时 loop；`get_state`
   同步调用在主线程抛 InvalidStateError → 一律 `aget_state`；pytest 多循环
   环境强制 memory 后端（生产 uvicorn 单循环无此问题）
2. **Reducer 签名**：langgraph 要求 (current, update) 双参 → 用 operator.add + 每轮写 1
3. **BrowserSession 跨循环失效**：playwright 连接绑定旧 loop → 检测 loop id
   变化自动重建会话（uvicorn --reload 同样受益）
4. **Docker Desktop 重启丢自定义网络**：compose down + up 重建（数据卷保留）

## 十三、W6 Collector 深度集成实测记录（2026-08-10）

**交付**：
- `services/rss_service.py`：feedparser 封装（RSS/Atom 自动识别，条目规范化
  title/link/published(ISO)/summary/source_url；非法/空提要明确报错）
- `agents/workers/collector.py` 重写：**三工具 ReAct 路由** —— `fetch_rss_tool`
  （首选）/ `fetch_static`（httpx 静态）/ `collect_webpage`（浏览器兜底）；
  RSS 特征注入系统提示词引导；工具失败 LLM 自动降级 = 「回退机制」落地
- `core/browser_agent.py`：拟人化时序（wait_between_actions=0.5s、
  加载等待 1s、网络空闲 2s）+ **连接类失败自动换代理重建会话重试**
  （collector_max_proxy_retries=2 可配，0=关闭；_is_connection_error 判定）
- `collect(source)` 三路由：auto（RSS 特征分流，不耗浏览器）/ rss / web；
  web 路径默认强类型 `WebExtract` 输出（W6 行为变更：W3 的纯文本默认改为结构化）
- API `POST /collect` 增加 `source`；内网 SSRF 拦截规则对 RSS 同样生效

**实测链路（全部通过）**：
- API：ChinaDaily 真实外网 feed（source=rss）→ **100 条结构化条目**（ISO 时间）✅
- CLI：本地 feed 强制 rss / auto 自动分流 ✅；内网 URL API 层 422 拦截 ✅
- pytest **72/72**（新增 10 例：RSS 解析/非法 feed/特征识别/换代理重试/三路由集成）

**测试稳定性处理**：全量连续 ~20 次真实 LLM 调用会触发网关 429 限流
（单跑均通过）→ ① LLM 客户端 max_retries=4；② 高频 LLM 用例标记
`@pytest.mark.flaky(reruns=2, reruns_delay=5)`（pytest-rerunfailures）；
全量 72/72 实测含 1 次限流自动重试通过

## 十四、双模型配置扩充记录（2026-08-10）

**配置**：`.env` 新增 `LLM_MODEL_LITE=sensenova-6.7-flash-lite`（同网关
`token.sensenova.cn/v1`，OpenAI 兼容）。主模型 `LLM_MODEL=deepseek-v4-flash`
不变（复杂推理/分析/浏览器操作/结构化路由）。

**实测结论（lite 能力边界）**：
- ✅ 普通 chat / **tool calling** 完整可用（get_weather 工具调用返回正确参数）
- ❌ **`with_structured_output` 不可用**：网关侧用 guided_grammar（xgrammar）
  约束结构化输出但服务端缺 xgrammar 模块 → HTTP 400
  `compile_grammar_error: No module named 'xgrammar'`

**接入点（按能力边界分配）**：
- Supervisor 路由：保持主模型（结构化输出稳定性优先）
- browser-use **judge_llm**（任务完成判定，普通 chat 二分类）：lite 模型
  `build_judge_llm()` —— 已实测浏览器采集链路兼容 ✅
- 未来轻任务（摘要/分类/错误归因）可继续使用 `settings.llm_model_lite`

## 十五、W7 GraphRAG 实测记录（2026-08-10）

**交付**：
- `services/entity_extraction.py`：领域本体（Company/Technology/Person/Paper/Event +
  DEVELOPED/INVESTED_IN/AUTHORED）+ 主模型 with_structured_output 三元组抽取
  （few-shot 示例后实体召回恢复：Milvus/LangGraph/BAAI-bge-m3 等正确识别）
- `services/graph_service.py`：幂等索引（name/type + 全文索引）、参数化 UNWIND
  **MERGE** 批量写入（防注入；doc_id 保留首发来源防共享节点被覆盖误删）、
  1-2 跳路径查询 → 自然语言段落
- `services/rrf.py`：RRF 融合（k=60，计划公式）；并列分数下图谱路径优先
- `retrieval_service` 升级 hybrid：向量 Top-20 + 图路径 → RRF → Top-5；
  图失败/空库自动退化纯向量；SourceHit 增加 source_type
- 入库管线自动建图（上传即建图，graph_count 字段）；research worker 接 hybrid

**实测链路（全部通过）**：
- 上传新版演示白皮书 → 自动抽取 **10 实体 + 2 关系**
  （`InsightAI -[DEVELOPED]-> 多智能体情报分析平台`、`张伟 -[AUTHORED]-> 白皮书`）
- API 混合问答：含实体锚点查询（"平台和 InsightAI 什么关系"）→
  **图路径进入 Top-1** `[graph] InsightAI--[DEVELOPED]-->平台`，回答带 [1][2][4][5] 引用
- 无实体锚点查询（"谁开发了平台"）→ 图检索固有边界，正确退化为纯向量（回答仍准确）
- pytest 84/84（新增 12 例：RRF 数学/抽取 Schema/图写入查询/端到端 hybrid）

**踩坑记录**：
1. Neo4j 驱动 6.2 `RETURN path` 返回**扁平 list**（节点与关系类型交替），
   非 Path 对象 → `_parse_path` 兼容三种形态；关系类型需显式
   `[r IN relationships(p) | r.type]` 带出（否则只显示标签 REL）
2. **MERGE SET 覆盖 doc_id 致共享节点被误删**（关键）：同名实体跨文档
   被后写文档覆盖 doc_id，测试按 doc_id 清理时误删演示数据 →
   `coalesce` 保留首发来源
3. lite 模型实体提取用**中文逗号**分隔 → split 兼容中英文标点 + 提示词
   排除泛指词（平台/报告/白皮书）
4. RRF 并列分（k=60 下 vector rank1 与 graph rank1 同分）图路径被 Top-N
   截断挤出 → 并列时图谱显式事实优先
5. SQLite 开发库 schema 迁移：create_all 不补列 → 手动 ALTER 加 graph_count

## 十六、W8 HITL 人机协作实测记录（2026-08-10/11）—— 阶段二里程碑

**交付**：
- 图改造：`human_review` 节点（`interrupt()` 挂起，payload=报告草稿）+ 条件边
  approve/reject→END、revise→analyst（携带意见重写）+ **修订熔断 MAX_REVIEWS=3**
  （超限自动批准）；analyst 提示词注入审核意见（针对性修订不推翻重写）
- API：`POST /agents/runs/{id}/review`（approve/reject/revise+comment，仅
  awaiting_review 可审 409）；状态机 `running → awaiting_review → ready/failed`；
  stream 推送 `review_required` 事件（含草稿，修订后 draft 变化重推）
- 恢复限流韧性：`_continue_run` 遇 429 置回 awaiting_review（可重审）而非判死
- 前端：/agents 任务页（阶段事件流 + ReviewCard：react-markdown 报告 +
  批准/修改意见/拒绝三按钮 + 意见输入）；chat 页与 /agents 导航互链；
  新增依赖 react-markdown + @tailwindcss/typography

**实测链路（全部通过）**：
- GUI 走查（浏览器自动化+截图+识图断言）：登录 → 发起任务 → 阶段事件
  （supervisor→research→analyst→finish）→ **审核卡出现**（Markdown 报告 +
  三按钮）→ 批准 → 「最终分析报告（已批准）」终态渲染 ✅
- pytest 91/91（新增 7 例：熔断/Reducer/API 鉴权/404/422/approve 流/revise 流）

**关键踩坑**：
1. **HTTPS_PROXY 环境变量干扰 pytest 内 grpc 连 Milvus**（pymilvus 走代理
   失败 → 探测 False → 集成测试全 skip）：无代理跑测试即恢复；conftest 探测
   加重试 + require_infra 运行时 fixture（避免 pytest 双加载 conftest 常量不一致）
2. `Command(resume)` 恢复执行遇 429：不能简单判死任务 → 置回待审核可重审
3. research.py 重构残留 `name 'h' is not defined`（W7 改坏行）→ 修复
4. tailwind.config 被脚本改坏 → 规范 ESM 重写
5. npm registry 直连被拒 → pnpm 配 npmmirror 镜像

## 十七、采集能力增强（W9 Sprint）：国内全平台化 + 反爬工程化（2026-08-11）

**调研依据**（技术博客/CSDN/知乎 + GitHub stealth 生态）：反爬分层
HTTP头 → TLS/JA3指纹 → JS挑战（加速乐/瑞数）→ 浏览器指纹（webdriver/
Canvas/WebGL）→ 行为分析 → 验证码 → 登录墙。

**实施**：
- `services/tls_fetch.py`：**curl_cffi**（impersonate=chrome、HTTP/2、会话按域
  复用、Chrome 完整请求头）—— 解决 requests/httpx TLS 指纹被 403 的根因；
  ⚠️ Windows 非 ASCII 项目路径下 CAfile 加载失败（curl 77）→ CA 放系统 Temp
- `core/stealth_browser.py`：persistent context（headless=new + viewport/
  locale/timezone + remote-debugging-port）→ **stealth JS 注入**（webdriver
  隐藏/chrome 补全/plugins/languages/并发修正）→ CDP ws 端点 →
  browser_use BrowserSession(cdp_url=...) 复用（Agent 流程零改动）；
  独立持久 profile（登录态跨任务）；`COLLECTOR_BROWSER_PROFILE=default`
  预留本机 Chrome 登录态复用
- `core/captcha_guard.py`：验证码检测（URL/标题关键词 + 滑块 DOM 特征）+
  截图归档 data/captcha/ + 产物 {status:"captcha"}（**检测+人工接管，不破解**）
- `services/anti_bot.py`：站点策略注册表（百度系 tls / 知乎·B站·微博 browser /
  豆瓣·电商 captcha 常发）+ 礼貌采集（同域随机 2-5s 节流）
- collector 适配：auto 路由 = RSS → TLS → 浏览器（策略驱动）；403/验证页
  明确回退信号保留

**实测（指纹验证 + 站点矩阵）**：
- Stealth 指纹：`webdriver=false, plugins=5, languages=zh-CN, cores=16, chrome✓`
- 站点矩阵 **8/8**：百度百科/CSDN/IT之家/博客园/简书/豆瓣(TLS <1s) +
  B站/知乎(Stealth 浏览器兜底)
- 百度百科：httpx 403 → curl_cffi 200（63KB）
- 测试 12/12 新增（策略/验证码/stealth/tls 路由/auto 优先 tls）

**合规**：新增 `docs/采集伦理与合规.md`（遵守 robots/低频/仅公开信息/
本机账号/不破解验证码）

## 十八、W9 FastMCP 服务化重构（2026-08-11）

**交付**：
- `mcp_servers/` 四个独立 FastMCP Server（Streamable HTTP，复用 backend venv）：
  BrowserMCP(8101: collect_webpage/fetch_static/fetch_rss + browser://health)、
  VectorStoreMCP(8102: knowledge_search/store_chunk + vector://collections)、
  GraphStoreMCP(8103: graph_query + graph://stats)、SystemConfigMCP(8104: 纯资源)
- `services/mcp_registry.py`：Client 握手（超时 5s + 重试 2 次 + 宕机端点隔离）、
  远端 inputSchema → Pydantic 参数模型 → langchain BaseTool 包装 `build_langchain_tools`
- `agents/tool_factory.py`：**双模式按工具合并**（远端优先 + 本地缺口补齐）——
  VectorMCP 停服实测 knowledge_search 自动回退本地，能力不中断
- API：GET /system/mcp 诊断 + POST /system/mcp/refresh 热插拔（JWT）
- 修复存量 bug：stream_sse 的 done 取态改 `aget_state`（AsyncRedisSaver 主线程
  同步调用限制，生产 redis 模式必现）

**实测链路（全部通过）**：
- 握手：4/4 端点 ok，6 远端工具（死端点 502 隔离不阻塞）
- MCP 调用：chat 触发 knowledge_search → 远端 VectorStoreMCP 返回 → 回答（事件流
  tool_start→tool_end→done 无错）
- 回退：Stop VectorMCP → refresh → 本地补齐 → 同问题回答正常
- 测试 5/5 新增（schema 适配/内存 transport 调用/MCP 优先/本地回退/鉴权）

**踩坑记录**：
1. `--port` CLI 参数不生效（FastMCP run 走 kwargs）→ `mcp.run(host, port)` 显式传
2. StructuredTool 参数传递：`args_schema=None` 时函数签名推断丢 **kwargs
   （input_value={}）→ 用远端 inputSchema 显式 create_model
3. 合并列表里混入 `mcp.types.Tool`（未包 BaseTool）→ ToolNode create_tool 报错
   → 先 build_langchain_tools 再按名合并
4. registry.ready 缓存历史状态 → 端点清空时显式判 `endpoints` 走本地
5. .env echo 追加粘行 → MCP_SERVERS 成注释（手动修正）

---

# §19 W10 性能优化 + 覆盖测试 + 压测报告

**日期**：2026-08-11

## 1. 输出校验 Guardrails（幻觉防护）
- `services/guardrails.py`：`extract_citations` / `validate_report_citations`（引用超标=幻觉信号）/
  `route_validator`（路由枚举校验）/ `track_hallucination_signal`（写入 Trace）
- 接线：Analyst 报告生成后引用校验 → 告警 + Trace 记录；纯函数可单测（9 用例）

## 2. 本地 Trace 双轨（LangSmith 可选）
- `services/trace_logger.py`：环形缓冲（200 条）+ 工具延迟分位（P50/P95/P99）+
  `tool_fail_ratio` + 幻觉信号计数；`GET /system/trace` 诊断端点
- LangSmith：配置 `LANGCHAIN_API_KEY` + `LANGCHAIN_TRACING_V2=true` 自动启用真实 Trace，
  无 key 时本地兜底（`langsmith_enabled` 标志）

## 3. Prompt few-shot 补全
- Supervisor 路由决策示例（含「未提及网址不路由 collector」反例）
- Chat SYSTEM_PROMPT 工具调用顺序示例（检索→回答带引用）
- Analyst 报告结构示例（概述/关键发现/时间线/结论）

## 4. 测试覆盖率 65% → 80%（目标达成）
- 新增/增强 11 个测试文件、69 用例（总 165 通过 + 13 flaky 真实 LLM/浏览器）
- 新文件 guardrails/trace_logger/human_review/supervisor 达 100%
- 关键修复（覆盖测试暴露）：agent_service 37%→93%（stream_sse 全分支）、
  mcp_registry 49%→97%（HTTP 握手/失败隔离/下线清理）
- 固化：pyproject `cov_fail_under = "80"`

## 5. 压测驱动性能优化（P50 6.5s → 0.28s）
- **Neo4j driver 复用**：每查询新建 driver（~1s 握手）→ 模块级单例连接池
- **向量/图谱并行**：`ThreadPoolExecutor(2)`，延迟从串行相加改为取较大者
- **实体提取缓存**：lite LLM 实体提取（限流时 2-17s）→ 同查询 5min TTL 缓存（6ms）
- 4 档 QPS（1/5/10/20）100% 成功率；报告 `docs/benchmark-report.md`

## 6. Neo4j EXPLAIN 索引验证
- `scripts/verify_neo4j_indexes.py`：三类查询全命中索引
  （NodeIndexSeek RANGE entity_name / FULLTEXT graph_entity_names / 路径起点索引）
- 顺带修复 deprecation：`CALL { WITH n }` → `CALL (n) { }`（Neo4j 5.26）

## 7. 覆盖测试暴露并修复的产品 Bug
1. `trace_logger.tool_fail_ratio` 恒 0（细分计数缺失）
2. `mcp_registry._run` content 缺 text 属性崩溃
3. `MCPRegistry.endpoints` 不去两端空白
4. **`browser_agent._get_session` 死锁**：run_coroutine_threadsafe().result(30) 在 loop
   内同步阻塞 → stealth 协程永不调度 → 每次建会话白等 30s（stealth 实际从未生效）→ async 化
5. **`stream_sse` error 后仍发 done**（空 answer）→ error 后终止流

## 8. 新增脚本
- `scripts/bench_retrieval.py`（4 档 QPS 压测 + 阶段拆解 + JSON 输出）
- `scripts/verify_neo4j_indexes.py`（EXPLAIN 索引命中验证）
- 两份报告：`docs/coverage-report.md` / `docs/benchmark-report.md`

---

# §20 W11 Docker 全栈编排 + CI/CD + 本地 Trace 面板

**日期**：2026-08-11

## 1. Docker 应用容器化（多阶段，防镜像过大）
- `backend/Dockerfile`：deps 阶段 uv sync + Playwright Chromium 下载；runtime 阶段拷 venv +
  ms-playwright + 源码 + chromium 系统库；healthcheck `/api/v1/health`
- `frontend/Dockerfile`：Next.js 14 **standalone** 输出（`next.config.mjs` 加 `output:"standalone"`），
  deps→build（corepack pnpm + NEXT_PUBLIC_API_BASE 注入）→runner
- 单一后端镜像同时承载 4 个 MCP Server（compose 覆盖 command + `--host 0.0.0.0 --port 810X`）
- `.dockerignore`：排除 venv/node_modules/.next/.coverage/数据库/测试日志/.env

## 2. docker-compose.yml 全栈编排（5 基础设施 + 6 应用容器）
- backend：env_file .env + environment 覆盖容器内主机名（milvus-standalone/neo4j-graph/
  redis-state-store）、MCP_SERVERS 覆盖为容器服务名、DATABASE_URL 落持久卷
- frontend：NEXT_PUBLIC_API_BASE=http://localhost:8000/api/v1（浏览器端直连）
- mcp-browser/vector/graph/system：8101-8104，`--host 0.0.0.0`（容器间经服务名访问）

## 3. GitHub Actions CI（.github/workflows/ci.yml）
- backend job：setup-uv → uv sync → ruff lint → compose 起基础设施（--wait）→ pytest 非 flaky + cov≥80%
- frontend job：pnpm lint + build（standalone）
- docker job：multi-stage 构建 backend/frontend 镜像

## 4. ruff 引入与存量清理
- `uv add --dev ruff` + pyproject [tool.ruff]（line-length 100，ignore B008/S110/RUF001-003/E501/
  TRY003/S105/S311/S324/TRY301——中文项目与业务惯例豁免）
- 手动修复 16 项语义问题：B007/PERF102（for values()）、G201×5（logger.exception 并去掉冗余
  exc 实参）、B904×2（raise from）、RUF006（后台任务引用集 _bg_tasks）、DTZ005/UP017、
  B905（zip strict）、PIE810、TRY300×2、TRY004（TypeError）

## 5. 本地 Trace 面板（零新依赖）
- `GET /system/trace/ui`：自包含 HTML（事件统计/失败率/延迟分位/guardrail 信号/最近事件），
  首帧服务端渲染，JS 读 localStorage insight_token 每 3s 轮询 /system/trace

## 6. 踩坑记录
1. Next.js standalone + pnpm 在 **Windows 本地 build 报 EPERM symlink**（需开发者模式）；
   Docker/CI（Linux）无此问题 → 文档注明
2. 4 个 MCP server 硬编码 bind 127.0.0.1 → 容器内其它容器无法访问 → 入口加 `--host` 参数
3. MCP server 默认端口 8000 与 backend 冲突 → compose 一律 `--port 810X`
4. docker compose up 前需停本地占用 8000/3000/8101-8104 的进程

## 7. 阶段 B 预留（腾讯云 CloudBase）
- 镜像已就绪（multi-stage）；部署步骤待 CloudBase 工具链接入后执行（单独一轮）

## 8. W11 验证结论（Docker 容器实测）
- backend 容器 healthy + `/api/v1/health` ✓
- 4 个 MCP 容器（8101-8104）healthy；容器内 `POST /system/mcp/refresh` 握手 4/4 ok、6 工具
  （MCP_SERVERS 覆盖为容器服务名 + mcp server `--host 0.0.0.0` 生效）
- RAG 链路：上传 PDF → 入库（Milvus 向量 + Neo4j 图谱）→ 混合检索问答（vector+graph 双源召回）✓
- chat → 远端 MCP knowledge_search（tool_start→tool_end→token→done）✓
- Trace 面板 `/api/v1/system/trace/ui` 渲染正常 ✓

## 9. W11 网络踩坑（Docker 镜像拉取）
1. **ghcr.io 拉 uv 镜像 <20KB/s** → 弃用 `COPY --from=ghcr.io/astral-sh/uv`，改 `pip install uv`
2. **BuildKit 不读 daemon.json 的 registry-mirrors**（独立镜像解析）→ Dockerfile 基础镜像
   ARG 化（`ARG BASE_PY/BASE_NODE`），本地 `.env` 的 `DOCKER_BASE_NODE` 覆盖为
   `docker.1ms.run` 加速镜像；CI 用默认官方镜像
3. **pip/pnpm/playwright 国内源**：`UV_DEFAULT_INDEX` 清华 + `PLAYWRIGHT_DOWNLOAD_HOST`
   npmmirror + `npm config set registry npmmirror`（均已写入 Dockerfile）
4. **Next.js standalone + pnpm 在 Windows 本地 build 报 EPERM symlink**（需开发者模式）；
   Docker/CI（Linux）无此问题
5. 验证时 frontend 容器本地构建因网络（node 基础镜像 43MB 拉不动）未完成，交由 CI
   （GitHub runner 网络正常）构建；本地用 `pnpm dev` 开发
